<%- headers = ['mongodb_id', 'start_date', 'end_date','active','credit_card_id','condition_id'] -%>
<%= CSV.generate_line headers %>
<%- @coefficients.each do |coef| -%>
  <%= CSV.generate_line([coef.id.to_s, coef.start_date, coef.end_date, coef.active, coef.credit_card_id.to_s, coef.condition_id.to_s]).html_safe %>
<%- end -%>